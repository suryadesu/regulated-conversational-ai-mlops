# TODO(build): gateway base; add configMapGenerator mounting prompts/<name>/<PROMPT_VERSION>.yaml (hash-suffixed).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - rollout.yaml
  - service.yaml
  - analysis-template.yaml
  - scaledobject.yaml
  - canary-prober-job.yaml
