# TODO(build): observability base (OTel collector; Grafana dashboard mounted via configMapGenerator at build time).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - otel-collector.yaml
