# TODO(build): ClusterIP Service for the provider stub on port 8080.
apiVersion: v1
kind: Service
metadata:
  name: provider-stub
spec:
  selector:
    app: provider-stub
  ports:
    - name: http
      port: 8080
      targetPort: 8080
