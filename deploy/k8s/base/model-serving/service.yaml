# TODO(build): ClusterIP Service for the serving pods (OpenAI-compatible port 8000).
apiVersion: v1
kind: Service
metadata:
  name: model-serving
spec:
  selector:
    app: model-serving
  ports:
    - name: http
      port: 8000
      targetPort: 8000
