#!/usr/bin/env bash
# TODO(build): implement. Enumerated final content:
#   kind create cluster (single node).
#   install KEDA, Argo Rollouts, and kube-prometheus-stack (helm).
#   install the Argo Rollouts kubectl plugin note.
set -euo pipefail

echo "TODO(build): bootstrap-local.sh not implemented"
